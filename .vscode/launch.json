{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch App",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "http://localhost:3000/thermal",
            "webRoot": "${workspaceFolder}"
        }
    ]
}